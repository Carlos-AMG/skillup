extends ../layout/index

block contenido
    h2.mt-5.text-2xl For 
        span.font-extrabold #{type}
    .py-10 
        h2.text-center.text-4xl.font-extrabold.text-green-500= pagina

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error? "bg-red-500":"bg-green-500").py-2.px-5.rounded-lg.w-full.font-bold.text-white.text-center.uppercase= mensaje

        if !error
            a(href="/students/login" class= "text-center font-bold text-sm text-green-600 uppercase mt-10 block") Sign in